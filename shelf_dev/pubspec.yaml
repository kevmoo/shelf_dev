name: shelf_dev
version: 1.0.0-dev
repository: https://github.com/kevmoo/shelf_dev/

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  build_cli_annotations: ^2.0.0
  checked_yaml: ^2.0.1
  json_annotation: ^4.0.0
  path: ^1.7.0
  shelf_proxy: ^1.0.0

dev_dependencies:
  build_cli: ^2.0.0
  build_runner: ^1.0.0
  json_serializable: ^4.0.2
  pedantic: ^1.9.0
  # Needed because we are configuring `combining_builder`
  source_gen: ^0.9.5
  test: ^1.6.0

dependency_overrides:
  # Need to update dependencies on these packages
  checked_yaml: ^2.0.1
